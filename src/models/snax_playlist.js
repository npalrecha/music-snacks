var snax_playlist = function(args) {
    args = args || {};

    var self = {
        artist: args.artist || '',
        album: args.album || '',
        title: args.title || '',
        duration: args.duration
    };
};

module.exports = snax_playlist;

